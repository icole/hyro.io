{{#if this.web3.locked}}
  <MetamaskLocked />
{{else if this.web3.wrongNetwork}}
  <MetamaskWrongNetwork />
{{else if this.web3.account}}
  <LogIn @model={{@model}} />
{{else}}
  <MetamaskMissing />
{{/if}}